# 下層ページの移行 — 記録

更新日: 2026-08-17

## いまの状態

| ファイル | 役割 | 行数 |
|---|---|---|
| `base.css` | 書体、トークン、素地、文字、版面、ボタン、ヘッダー、フッター | 870 |
| `system.css` | 章の面、章の入口、図版、写真の帯、一覧、表、注記 | 471 |
| `top.css` | index.html だけの部品 | 317 |
| `styles.css` | 各ページ固有の部品（下の「なぜ残すか」を参照） | 1,276 |
| **合計** | | **2,934**（移行前 3,236） |

## 済んだこと

### 段階0 — 共通レイヤーの確立

`top.css` から「2ページ以上で使う語彙」を `system.css` へ切り出しました。
index.html の見た目は変えていません（分割前後の全画面比較で差分0画素）。

### 段階1〜3 — 全21ページの移行

| 旧 | 新 | 状態 |
|---|---|---|
| `.band` / `.band--surface` / `.band--ink` | `.sec` / `.sec--paper2` / `.sec--ink` | **完了（残り0）** |
| `.idx-head` | `.opener` | **完了（22章／残り0）** |
| `.container` | `.wrap` | HTMLは完了。JS生成分のみ名前を残し `.wrap` と同寸に統一 |

章の英字22個は、すべて見出しと突き合わせて手で決めました。

### 段階4 — 掃除

- `.band--ink` の旧名73か所を撤去（`.sec--ink` へ一本化）
- `styles.css` から未使用242ルールを削除（1,601行 → 1,276行）
  **削除前後で全21ページのピクセルを比較し、1pxも変化しないことを確認済み**
- `.js .reveal { opacity: 0 }` を廃止（下の「危険だった指定」を参照）

## なぜ styles.css を残すか

残っている787ルールは、**各ページ固有の部品**です。

`.plan-cell` `.plan-table` `.timeline` `.cycle` `.spec-card` `.faq-item` `.map-cell`
`.trend` `.mode-card` `.rs-toc` `.legal__toc` `.article__related` など。
1ページでしか使わないものが大半で、`system.css`（2ページ以上で使うもの）の
基準に合いません。**移しても行数は減らず、置き場所が変わるだけです。**

いま `styles.css` は「旧システム」ではなく「ページ固有の部品置き場」になっています。
役割が変わったので、名前を `pages.css` へ改めるのが正確ですが、
参照が21ページにあるため、改名は別の作業として扱います。

## 危険だった指定

`base.css` に次の対がありました。

```
.js .reveal { opacity: 0; transform: translateY(18px); }   /* 643行目 */
.js .reveal, .js .reveal.is-in { opacity: 1 !important; }  /* 817行目 */
```

`.reveal` は下層ページで使われていないため、機械的な未使用判定では
**817行目だけが削除対象**になります。すると643行目が生き残り、
`.reveal` を持つ要素が全ページで透明のままになります。

**要素をいったん透明にする作りは、打ち消し規則と対でしか成立しません。**
初期値を透明にする指定そのものを廃止しました。

## 移行中に起きた失敗

1. **閉じタグの取り違え。** `<div class="idx-head">(.*?)</div>` は入れ子の `<div>` を見ず、
   包んでいた `.container` の閉じタグまで飲み込んでいました。開閉を数える方式に変更。
2. **濃色面の指定が全部外れた。** `styles.css` の73か所が `.band--ink` に紐づいており、
   `.sec--ink` へ改名した瞬間に無効化。数字が 1.03:1 まで落ちました。
3. **濃色面の中の白いカードで、白地に白文字。**
   `system.css` に「白地のカードの中は淡色面に戻す」規則を追加。
4. **未使用削除で `@font-face` を巻き込んだ。**
   セレクタにクラス名がない規則を「未使用」と判定していました。
   書体が全ページで失われ、差し戻し。`base.css` と `system.css` の削除は撤回しています。

## 検査（全21ページ × デスクトップ／モバイル）

- コントラスト不足 0件
- 横方向のはみ出し 0件
- JavaScriptエラー 0件
- 書体の読み込み 全ページ正常
- 透明のまま残る本文 0件
- 旧クラス `.band` `.idx-head` `.band--ink` の残り 0件
- 診断の通し動作 正常（16問開始 → 回答 → 座標更新 → 結果表示 高さ8,975px）
- index のスクロール中の総高さ 一定
