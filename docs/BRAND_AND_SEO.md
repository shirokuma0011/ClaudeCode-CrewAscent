# ブランド表記と「Web診断36」の設計

更新日: 2026-08-17

## 1. ロゴ

### 資産

| ファイル | 用途 |
|---|---|
| `assets/brand/logo.png` / `.webp`（＋`@2x`） | 大きく見せる場面。写真からの正本。淡色面用 |
| `assets/brand/logo-onink.png` / `.webp`（＋`@2x`） | 濃色面用。Aを白へ置き換えた版 |
| `assets/brand/logo-mark.svg` | ヘッダー・フッター・ファビコン（20〜48px） |
| `assets/brand/logo-512.png` | OGP・SNS・印刷の原本 |

### 使い分けの理由

写真の正本は、上昇矢印と三点経路まで含んだ完全な形です。ただしその細部は
**40px前後で潰れて判読できません。** そのためヘッダーとフッターには、
**2026-08-17 変更**: 正式マーク（`assets/brand/logo.*` / `logo-onink.*`）を採用しました。
上昇矢印と三点経路はブランドの意味そのものなので、簡略版ではなく正本の絵を表示します。
ヘッダーの高さに合わせて 54〜66px で表示し、濃い地では A を白の輪郭にした `logo-onink` へ
CSS が自動で切り替えます（背景画像なので、実際に使う方だけを取得します）。
簡略SVG `logo-mark.svg` は参考として残していますが、どこからも参照していません。

簡略マークは `currentColor` で描いているので、濃色面では自動的に白へ反転します。
CとAのあいだの隙間は `--lg-bg`（地の色）で描いており、面の色が変わっても隙間が保たれます。

### 触ってはいけないこと

- 簡略マークの比率（viewBox 128×120）を変えない
- CとAの重なり量を変えない。読みやすさはこの重なりで決まっている
- 濃色面で `logo.png`（淡色用）を使わない。濃紺のAが背景に溶ける

---

## 2. サービス名「Web診断36」

### 表記の規則

| 場面 | 書き方 |
|---|---|
| サービス名として | **Web診断36**（半角英字＋全角漢字＋半角数字。中黒や空白を入れない） |
| 説明を添えるとき | Web診断36（36タイプのWebサイト方向性診断） |
| 行動の呼びかけ | 「無料診断を始める」のままでよい。名前を無理に入れない |
| 英語表記 | Web Shindan 36（英語ページを作るときに確定する） |

**「36タイプ診断」は名前ではなく説明です。** 初出の近くで補足として残し、
名前として使う場所は「Web診断36」に統一しました。

### この名前で検索されたときに届くようにした設計

1. **`diagnosis.html` を正本のページとした。**title の先頭に置き、h1 の第一行にも置いています
2. **h1 の構造**を「Web診断36（サービス名）＋説明文」の2段にしました
3. **構造化データ**を追加しました
   - `diagnosis.html` … `WebApplication`（name: Web診断36、無料、日本語、機能一覧）
   - `diagnosis-guide.html` … `HowTo`（4手順）
   - `faq.html` … `FAQPage` の先頭に「Web診断36とは何ですか？」
4. **FAQ に定義を1件追加**しました。名前だけで検索した人が、最初に意味へたどり着けます
5. **内部リンクの文言**を「Web診断36」に揃えました（ナビ、メガメニュー、フッター）
6. 全21ページに名前が入り、総出現数は113です

### やっていないこと

- 語の詰め込みはしていません。行動の呼びかけは日本語として自然な形を優先しました
- 検索順位は保証できません。**新語なので競合は少ないですが、
  上位に出るかは公開後の被リンクと利用実績に依存します**

### 公開後にやるべきこと

- Google Search Console でサイトを登録し、`Web診断36` の表示回数を確認する
- 名前を外部（SNS、名刺、見積書）でも同じ表記で使う。表記ゆれが最大の敵です
- 誤記（Web診断３６、WEB診断36、Web 診断 36）が増えたら、それらも本文のどこかで一度触れる

---

## 3. 濃色の色味

`--ink` を `#14161A`（中性の黒）から **`#0B1220`（ロゴ寄りの濃紺）** に変更しました。
`base.css` の1か所で定義しているので、全21ページに反映されています。
