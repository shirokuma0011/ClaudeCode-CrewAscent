import {sqliteTable,text,integer,index,uniqueIndex} from 'drizzle-orm/sqlite-core';
import {sql} from 'drizzle-orm';
export const slots=sqliteTable('slots',{id:text('id').primaryKey(),startsAt:integer('starts_at').notNull().unique(),endsAt:integer('ends_at').notNull(),active:integer('active').notNull().default(1),createdAt:integer('created_at').notNull()},t=>[index('idx_slots_active_start').on(t.active,t.startsAt)]);
export const bookings=sqliteTable('bookings',{id:text('id').primaryKey(),slotId:text('slot_id').notNull().references(()=>slots.id),name:text('name').notNull(),email:text('email').notNull(),notes:text('notes').notNull(),tokenHash:text('token_hash').notNull().unique(),requestKey:text('request_key').notNull().unique(),status:text('status').notNull().default('confirmed'),createdAt:integer('created_at').notNull(),cancelledAt:integer('cancelled_at'),privacyVersion:text('privacy_version').notNull()},t=>[uniqueIndex('idx_bookings_one_per_slot').on(t.slotId).where(sql`${t.status} = 'confirmed'`),index('idx_bookings_created_at').on(t.createdAt)]);
export const inquiries=sqliteTable('inquiries',{id:text('id').primaryKey(),requestKey:text('request_key').notNull().unique(),payload:text('payload').notNull(),createdAt:integer('created_at').notNull(),privacyVersion:text('privacy_version').notNull()},t=>[index('idx_inquiries_created_at').on(t.createdAt)]);
export const rates=sqliteTable('rates',{key:text('key').primaryKey(),count:integer('count').notNull(),expires:integer('expires').notNull()},t=>[index('idx_rates_expires').on(t.expires)]);
export const quotes=sqliteTable('quotes',{id:text('id').primaryKey(),tokenHash:text('token_hash').notNull().unique(),plan:text('plan').notNull(),customerType:text('customer_type').notNull(),email:text('email').notNull(),stage:text('stage').notNull(),amount:integer('amount').notNull(),total:integer('total').notNull(),scope:text('scope').notNull(),delivery:text('delivery').notNull(),expires:integer('expires').notNull(),createdAt:integer('created_at').notNull(),status:text('status').notNull().default('approved'),checkoutUrl:text('checkout_url'),orderId:text('order_id'),acceptedAt:integer('accepted_at'),contractVersion:text('contract_version').notNull(),paidAt:integer('paid_at')});
export const events=sqliteTable('events',{id:text('id').primaryKey(),topic:text('topic').notNull(),receivedAt:integer('received_at').notNull(),quoteId:text('quote_id'),status:text('status').notNull()});
export const outbox=sqliteTable('outbox',{id:text('id').primaryKey(),kind:text('kind').notNull(),payload:text('payload').notNull(),createdAt:integer('created_at').notNull(),sentAt:integer('sent_at'),attempts:integer('attempts').notNull().default(0),lastError:text('last_error')},t=>[index('idx_outbox_sent_at').on(t.sentAt)]);
