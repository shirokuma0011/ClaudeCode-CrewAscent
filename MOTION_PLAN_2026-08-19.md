# モーション復活 — 実行計画（全21ページ）

作成日: 2026-08-19

## 0. なぜ今、動きが無いのか

スクロールの加速問題を7回かけて追ったとき、原因の一つが
**「出現アニメーションが、ページ本体と違う速さで動くものとして見える」**ことでした。
その対策として `.rv` / `.rvm` / `.rvl`（出現・マスク・罫線）を
CSSで**全部止めました**。JSの発火装置（IntersectionObserver）は生きたままです。

現在の実測: キーフレームアニメ **0件**、動く transition は
ヘッダー・矢印・画像ホバーの **95か所（0.19秒級）のみ**。
`CLAUDE.md` のルール11「署名モーションで記憶に残す」は未達です。

## 1. 原則 — これを外すと同じ失敗を繰り返す

計測で分かっていた「加速して見える」の原因は2つでした。

| 原因 | 実測 | 動きを戻すときの意味 |
|---|---|---|
| 目印の無い区間が画面の58〜61%続く | 5区間で520〜550px | **本文や写真を隠す出現アニメは、目印を消すので禁止** |
| 要素がページと違う速度で動く | `.rv` の translateY(14px) | **スクロール軸（縦）に動かす演出は禁止** |

そこで、この2行を守ります。

> **本文・写真・見出しは、最初から最後まで見えている。動くのは「印」だけ。**
> **スクロールは縦。だから、動きはすべて横（左→右）。縦には動かさない。**

罫が左から伸び、英字が左から現れ、マスが左上から順に点く。
**縦方向の動きが1つも無ければ、スクロールと競合しようがありません。**

## 2. 4つの系統

| 系統 | いつ | 何が動く | 時間 | 面積 |
|---|---|---|---|---|
| **M1 Route** | 読み込み時・1回 | ヒーローの6×6格子・十字の罫・1マスの点灯 | 1,400ms以内 | 第一画面のみ |
| **M2 Mark** | 画面に入って1回 | 章の細罫（左→右）・章の英字（左から現れる）・図版の枠 | 500〜900ms | 罫と英字のみ |
| **M3 Shift** | 操作したとき | 開閉・タブ・設問のページ送り・ドロワー | 160〜240ms | 触った箇所 |
| **M4 Focus** | 図の中で完結 | 36マップの現在地・診断中の座標マーカー・図解の強調 | 300〜600ms | 図の中だけ |

使う性質は **`opacity` / `transform`（scaleX・translateX）/ `clip-path`** だけ。
`filter`・`box-shadow`・`width`・`top` は使いません（再描画が重いため）。

## 3. 安全装置（4重）

1. **スクロール中は M2 / M4 を止める** — 既存の `html.is-scrolling` を使う。
   高速スクロール中は一切動かない。止まってから動く。
   隠していないので、止まっている間も中身は読めます。
2. **`prefers-reduced-motion`** — 全系統が最終状態を即時表示（8/19に全体ガード実装済み）。
3. **切り替えスイッチ** — `site-config.js` の `motion: 'on' | 'off'`。
   URLに `?motion=off` でもその場で切れます。
4. **同時に動くのは1画面あたり3つまで**。ループ再生は無し。自動カルーセルも無し。

## 4. 全ページの割り当て

markup側の受け口（`.rv` / `.rvm` / `.rvl` / `.opener` / `.arch`）は
**すでに全ページに入っています**。足すのはCSSと、ごく一部のJSだけです。

| ページ | M1 | M2（罫/英字） | M3 | M4 | 備考 |
|---|---|---|---|---|---|
| index | ● 6×6+十字+1マス | 章扉7・罫8 | サービス4話の切替 | — | 唯一 M1 を持つ |
| services | — | 章扉4・罫4 | — | — | |
| plans | — | 章扉5・罫5 | 比較表のモバイルタブ | — | |
| process | — | ページ英字・罫 | — | 4区切りの現れ | 8/18に追加した区切り |
| management | — | 章扉2・罫2 | — | — | |
| diagnosis | — | 章扉1・罫1 | **設問ページ送り** | **FIG.01の36マス** | 中心。M3とM4が主役 |
| diagnosis-guide | — | 章扉4・罫4 | — | 四つの視点→2軸の図 | |
| result | — | ページ英字 | 折りたたみ12節 | **36マップの現在地** | 個人レポートの要 |
| guide | — | ページ英字・罫 | — | 記事3枚の画像 | |
| website-guide | — | 章扉4・罫4 | — | — | |
| 記事3本 | — | **英字のみ**（章扉は使わない） | 目次の現在地 | **図解2枚ずつの枠** | 読む場所。動きは最小 |
| about | — | 章扉2・罫2 | — | — | |
| faq | — | 章の罫5（8/18に追加） | **開閉20件** | — | |
| contact | — | 章扉1・罫1 | — | 3段階の流れ | |
| works | — | ページ英字 | — | — | 準備中 |
| 法務3ページ | — | **無し** | 追従目次の現在地 | — | 型Fは演出しない決まり |
| 404 | — | ページ英字 | — | — | |

**法務3ページと記事本文で M2 の章扉を使わない**のは、
`PAGE_ARCHETYPES.md` が「読む場所では動かさない」と決めているからです。
記事は英字1つと図解の枠だけ、法務は追従目次の現在地だけにします。

## 5. 作業手順

1. `base.css` / `system.css` の**停止指定を外す**（3か所）
2. `.rvm` を縦マスクから**横ワイプ**へ書き換え（縦の動きを消す）
3. `.rv` は**移動をやめ、`opacity` だけ**にする。ただし
   **写真・本文ブロックからは `.rv` を外す**（隠さないため）
4. `html.is-scrolling` で M2 / M4 を停止する指定を足す
5. ヒーローの6×6を背景画像から**インラインSVG**へ戻し、M1を1回だけ流す
6. `site-config.js` に `motion` スイッチ、`main.js` に `?motion=off` の受け口
7. 診断のページ送りに交差フェード、結果マップの現在地に1回の点灯
8. 計測して比較（下記）

## 6. 合格条件（数値）

| 項目 | 基準 | 測り方 |
|---|---|---|
| 落ちコマ | 改修前と同等（数百コマ中0〜2） | `handoff/.../perf.mjs` |
| 入力量に対する移動量 | 比 1.00±0.02（速度は変えない） | `scroll-speed-check.mjs` |
| 区間別の最悪フレーム | 改修前 +2ms 以内 | `scroll-jank-by-region.mjs` |
| 目印の無い区間 | 改修前と同じ（増やさない） | `scroll-fix` の走査 |
| 縦に動く演出 | **0件** | CSSを機械的に検査 |
| reduced-motion時に動くもの | **0件** | 実測 |
| 横スクロール | 147通りで0 | `overflow-check.mjs` |

**1つでも外したら、その演出は入れません。**

---

# 実施結果（同日）

## 変更したファイル

| ファイル | 内容 |
|---|---|
| `assets/css/system.css` | M1〜M4のCSS（末尾に追記）。停止指定はここで選び直す |
| `assets/js/top.js` | 速度の実測（`is-fast`）、`.dgm-mini` を出現の対象へ、骨格の切り抜き解除 |
| `assets/js/diagnosis-ui.js` | 設問ページ送りの重ね |
| `assets/js/site-config.js` | `motion: 'on' \| 'off'` |
| `assets/js/main.js` | `motion:'off'` の後追い反映 |
| 全21ページ | `<head>` の1行スクリプトに `motion` クラスの付与を追加 |

## 計画から変えた2点

**1. `.rv`（本文ブロックの出現）は戻しませんでした。**
`.rv` はグリッド・カード群・図版・リード文など**大きな内容**に付いています。
これを透明から出すと、スクロール中の目印が消えます。
目印の無い区間は「速く見える」の実測原因そのものでした。
実際、改修後も**目印の無い区間は改修前と1pxも変わっていません**（下表）。

**2. 「スクロール中は止める」を「速いときだけ止める」に変えました。**
既存の `is-scrolling` はスクロールしていれば常に付くため、
これで止めると**読む速さで送っているときも動かない**＝実質モーション無しでした。
実測で速度を測り、しきい値 1,400px/秒（読む速さ300px/秒、ホイール1ノッチの
瞬間値1,100px/秒、勢いよく送ると6,000px/秒）を超えたときだけ補間を切ります。

## 途中で見つけて直した失敗

**`will-change` を付けたら、全区間で p95 が倍になりました。**

| | 落ちコマ（p95 / >20ms） |
|---|---|
| 改修前 | 16.7〜16.8ms / 0件 |
| `will-change` あり | **33.3〜50.0ms / 各区間4〜8件** |
| `will-change` を外した後 | 16.7〜16.8ms / 0件 |

22要素に常時レイヤーを確保させたのが原因です。**外しました。**

**ヒーローの切り抜きも、付けっぱなしにすると重くなります。**
骨格を左から開く `clip-path` を残したままだと、スクロールのたびに
ヒーロー全面の切り抜き計算が走ります。演出が終わったら
`top.js` が `.arch-done` を付けて `clip-path: none` に戻します。

## 合格条件の結果

| 項目 | 基準 | 結果 |
|---|---|---|
| 落ちコマ | 改修前と同等 | 8ページ計 0〜2コマ／数百コマ（改修前 0〜1）**合格** |
| 入力量に対する移動量 | 比 1.00±0.02 | gentle/normal/fling とも **1.00**、最高速度も同値 **合格** |
| 区間別の最悪フレーム | 改修前 +2ms 以内 | 9区間すべて p95 16.7〜16.8ms（改修前と同値）**合格** |
| 目印の無い区間 | 増やさない | 400/360/350/320/320/250/150/150px = **改修前と完全に同じ** **合格** |
| 縦に動く演出 | 0件 | CSSを機械検査。使用は `opacity` / `transform` / `clip-path` のみ **合格** |
| reduced-motion時に動くもの | 0件 | 4ページで実測 **0件**、隠れたままの印も0 **合格** |
| 横スクロール | 147通りで0 | **0 合格** |
| 表示されないまま残る印 | 0件 | 全21ページを走査して **0件 合格** |

自動監査も改修前と同じ P0=0 / P1=1（既知の誤検出）/ P2=22 のままです。

## 切りたくなったら

- URLに `?motion=off`
- `assets/js/site-config.js` の `motion: 'off'`
- OSの「動きを減らす」設定（この2つに関係なく止まります）
